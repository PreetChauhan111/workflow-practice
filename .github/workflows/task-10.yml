name: Task 10

on:
  push:
    branches:
      - main
      - develop

jobs:
  main-only:
    if: github.ref_name == 'main'
    runs-on: ubuntu-latest
    steps:
      - name: Condition to main
        run: echo "Running on main branch"
        