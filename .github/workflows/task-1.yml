name: Task 1 

on:
    workflow_dispatch:

jobs:
    hello-ci:
        
        runs-on: ubuntu-latest

        steps:

            - name: Code Checkout
              uses: actions/checkout@v4
            
            - name: Printing outputs
              run: |
                echo "Hello GitHub Actions"
                echo "Repo: ${{github.event.repository}}"
                echo "Actor: ${{github.actor}}"
                echo "Workflow: ${{github.workflow}}"

